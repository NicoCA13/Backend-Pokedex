exports.getMovimientos = (req, res) => {
  // const { nombre } = req.params;
  // const { movimientos } = Pokemones.find(
  //   (p) => p.nombre.toLowerCase() === nombre.toLowerCase()
  // );
  // res.send(movimientos);
};
