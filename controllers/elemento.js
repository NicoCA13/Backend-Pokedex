exports.getElemento = (req, res) => {
  // const { nombre } = req.params;
  // const { elemento } = Pokemones.find(
  //   (p) => p.nombre.toLowerCase() === nombre.toLowerCase()
  // );
  // res.send(elemento);
};
