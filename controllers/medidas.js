exports.getMedidas = (req, res) => {
  // const { nombre } = req.params;
  // const { peso, altura } = Pokemones.find(
  //   (p) => p.nombre.toLowerCase() === nombre.toLowerCase()
  // );
  // res.send({ peso, altura });
};
