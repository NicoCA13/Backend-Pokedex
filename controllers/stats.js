exports.getStats = (req, res) => {
  // const { nombre } = req.params;
  // const { stats } = Pokemones.find(
  //   (p) => p.nombre.toLowerCase() === nombre.toLowerCase()
  // );
  // res.send(stats);
};
